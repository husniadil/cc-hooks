# Claude Code Hooks Configuration
# Copy this file to .env and customize values as needed

# Database Configuration
DB_PATH=events.db

# Server Configuration
HOST=0.0.0.0
PORT=12222
MAX_RETRY_COUNT=3

# TTS (Text-to-Speech) Configuration
# Comma-separated list of providers in order of preference (leftmost = highest priority)
# Available providers: prerecorded, gtts, elevenlabs
TTS_PROVIDERS=prerecorded

# Enable/disable TTS file caching (recommended: true)
TTS_CACHE_ENABLED=true

# Language code for TTS generation (ISO 639-1 format) - only affects gtts and elevenlabs
# Examples: en (English), es (Spanish), fr (French), de (German), ja (Japanese)  
TTS_LANGUAGE=en

# Examples for different TTS configurations:

# Use pre-recorded sounds only (default, fastest)
# TTS_PROVIDERS=prerecorded

# Use Google TTS with pre-recorded fallback (recommended for dynamic voices)
# TTS_PROVIDERS=gtts,prerecorded

# Use Google TTS only (no fallback)
# TTS_PROVIDERS=gtts

# Skip Google TTS, use ElevenLabs with pre-recorded fallback (when elevenlabs is implemented)
# TTS_PROVIDERS=elevenlabs,prerecorded

# Use all providers with full fallback chain
# TTS_PROVIDERS=elevenlabs,gtts,prerecorded

# ElevenLabs Configuration (requires API key subscription)
# Get your API key from: https://elevenlabs.io/docs/authentication
# ELEVENLABS_API_KEY=your_api_key_here

# Voice ID for ElevenLabs TTS (default: Rachel voice)
# Find voice IDs at: https://elevenlabs.io/app/voice-lab
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# Model ID for ElevenLabs (default: eleven_flash_v2_5 for speed)
# Available models: eleven_v3, eleven_flash_v2_5, eleven_turbo_v2_5, eleven_multilingual_v2, eleven_english_sts_v2
ELEVENLABS_MODEL_ID=eleven_flash_v2_5

# OpenRouter Configuration (for AI-powered translation and contextual messages)
# Get your API key from: https://openrouter.ai/keys

# Enable/disable OpenRouter integration (default: false)
OPENROUTER_ENABLED=false

# OpenRouter API key (required if enabled)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Default model for OpenRouter requests (default: openai/gpt-4o-mini)
# Popular options: google/gemini-2.5-flash-lite, openai/gpt-4o-mini, anthropic/claude-3-haiku
# For translations: google/gemini-2.5-flash-lite (fast, cheap), openai/gpt-4o-mini (reliable)
# See all models: https://openrouter.ai/models
OPENROUTER_MODEL=openai/gpt-4o-mini

# Contextual Message Generation (CAUTION: API costs apply!)
# These features use OpenRouter API to generate dynamic, conversation-aware messages
# Disable to control costs, enable for enhanced user experience

# Enable contextual Stop messages (default: false)
# Generates completion messages based on conversation context instead of generic "Task completed"
OPENROUTER_CONTEXTUAL_STOP=true

# Enable contextual PreToolUse messages (default: false)  
# Generates action-oriented messages like "Installing dependencies you requested" instead of "Running Bash tool"
OPENROUTER_CONTEXTUAL_PRETOOLUSE=false

# Translation Integration Examples:
# When TTS_LANGUAGE is not "en" and OPENROUTER_ENABLED=true, audio descriptions
# will be translated to the target language before being sent to TTS providers

# Example: Indonesian TTS with AI translation
# TTS_LANGUAGE=id
# TTS_PROVIDERS=gtts,prerecorded  
# OPENROUTER_ENABLED=true
# OPENROUTER_API_KEY=your_key_here

# Example: Spanish TTS with ElevenLabs and AI translation
# TTS_LANGUAGE=es
# TTS_PROVIDERS=elevenlabs,gtts,prerecorded
# OPENROUTER_ENABLED=true
# OPENROUTER_API_KEY=your_key_here

# Contextual Message Examples:
# Enable both contextual features with cost-effective model
# OPENROUTER_ENABLED=true
# OPENROUTER_API_KEY=your_key_here
# OPENROUTER_MODEL=google/gemini-2.5-flash-lite
# OPENROUTER_CONTEXTUAL_STOP=true
# OPENROUTER_CONTEXTUAL_PRETOOLUSE=true

# Enable only contextual PreToolUse messages (more frequent, lower individual cost)
# OPENROUTER_ENABLED=true
# OPENROUTER_API_KEY=your_key_here
# OPENROUTER_CONTEXTUAL_PRETOOLUSE=true

# Enable only contextual Stop messages (less frequent, good for task completion feedback)
# OPENROUTER_ENABLED=true
# OPENROUTER_API_KEY=your_key_here
# OPENROUTER_CONTEXTUAL_STOP=true
